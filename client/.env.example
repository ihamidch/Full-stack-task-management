# Public API base (no trailing slash). In local dev, leave unset to use Vite proxy to :5000.
# VITE_API_URL=http://localhost:5000

# Socket.io server URL (same host as API in production). For local dev, point at the API port:
# VITE_SOCKET_URL=http://localhost:5000
